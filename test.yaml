apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: tom-daemonset
spec:
  selector:
    matchLabels:
      name: tom-daemonset
  template:
    metadata:
      labels:
        name: tom-daemonset
    spec:
      hostPID: true
      tolerations:
        - effect: NoSchedule
          key: node-role.kubernetes.io/master
      containers:
        - image: nginx:latest
          name: tom-daemonset
      terminationGracePeriodSeconds: 30
this is my yaml file i want to add the Readiness Probe Should be Configured
