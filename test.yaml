apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: jerry-cronjob
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: jerry-container
            image: nginx:latest
            securityContext:
              capabilities:
                drop:
                  - NET_RAW
          restartPolicy: OnFailure
