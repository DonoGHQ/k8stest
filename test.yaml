apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: docker-socket-security
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: docker-socket-security
            image: nginx:latest
          restartPolicy: OnFailure
