apiVersion: v1
kind: PodTemplate
metadata:
  name: my-pod-template
spec:
  metadata:
    labels:
      app: my-app
  spec:
    containers:
    - name: my-container
      image: nginx:latest
      securityContext:
        privileged: false
