apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: disable-tiller-daemonset
  namespace: kube-system
spec:
  selector:
    matchLabels:
      app: helm
      name: tiller
  template:
    metadata:
      labels:
        app: helm
        name: tiller
    spec:
      containers:
      - name: dummy-container
        image: busybox:latest
        command:
        - /bin/sh
        - -c
        - sleep 365d
