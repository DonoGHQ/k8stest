apiVersion: batch/v1
kind: Job
metadata:
  name: jerry-job
spec:
  template:
    metadata:
      labels:
        app: app-jerry-job
    spec:
      containers:
      - name: job-container
        image: busybox:latest
        command: ["echo", "Hello, World"]
      restartPolicy: Never
  backoffLimit: 4
