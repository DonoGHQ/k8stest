apiVersion: v1
kind: PodTemplate
metadata:
  name: sample123
spec:
  metadata:
    labels:
      app: tom-app
  spec:
    containers:
    - name: tom-secret
      image: busybox:latest
      command:
        - kubelet
        - --anonymous-auth=true
