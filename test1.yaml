apiVersion: batch/v1
kind: Job
metadata:
  name: jobs-timeout-74pass
  namespace: test-kubequery-job
spec:
  activeDeadlineSeconds: 100
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: jobs-timeout-container74pass
        image: busybox:latest
        command:
        - kube-apiserver
